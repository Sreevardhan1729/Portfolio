"use client";

import { motion } from "framer-motion";

export const MotionDiv = motion.div;
export const MotionButton = motion.button;
export const MotionP = motion.p;

export const varaiants = {
  hidden: { opacity: 0 },
  visible: { opacity: 1 },
};
